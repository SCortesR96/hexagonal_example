import { environment } from '@environments/environment';

// Mock variables
export const MOCK_URL_BASE = environment.MockApi.URL_BASE;
export const MOCK_URL_PREFIX = environment.MockApi.URL_PREFIX;

// Media
export const MEDIA_ASSETS = environment.mediaSource.assets;
export const MEDIA_IMAGE_PATH = environment.mediaSource.imagesPath;
